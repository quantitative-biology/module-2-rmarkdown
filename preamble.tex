% These are latex packages to use
\usepackage{mathptmx}       % Gives a bolder font than the Latex default.
\usepackage[table]{xcolor}
\usepackage{colortbl}
\usepackage{booktabs}
\usepackage{wrapfig}
\usepackage{float}
\usepackage{colortbl}
\usepackage{pdflscape}
\usepackage{tabu}
\usepackage{threeparttable}
\usepackage{threeparttablex}
\usepackage[normalem]{ulem}
\usepackage{makecell}

% These are user-defined aliases. You can create your own for frequently used
%  latex commands or expressions.
\newcommand{\eb}{\begin{eqnarray}}
\newcommand{\ee}{\end{eqnarray}}

% Comment these out to add line numbering:
% \usepackage[pagewise]{lineno}     % if you want to add line numbers in

% \linenumbers
% \modulolinenumbers[5]   % just number every 5th line
